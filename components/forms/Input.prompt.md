Labelled text field; use for every form input. Label sits above; pass `error` for the red invalid state, `hint` for helper text.

```jsx
<Input label="E-mail" type="email" placeholder="Preencha o seu e-mail" />
<Input label="Telefone" error="Número inválido" />
```
